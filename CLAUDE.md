# cleanplots

The Claude skill for this library (`cleanplots`) lives in the dotfiles repo at
`claude/skills/cleanplots/` — it travels there because the library is often
pip/uv-installed on machines without this checkout. If you change the API
(plot-call signatures, `clean()` options, `cp.data` helpers) or the defaults,
update that skill to match.
